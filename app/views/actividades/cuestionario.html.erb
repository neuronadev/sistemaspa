 <script src="https://unpkg.com/knockout@3.5.1/build/output/knockout-latest.js"></script>
        <script src="https://unpkg.com/survey-knockout@1.8.77/survey.ko.min.js"></script>
        <link href="https://unpkg.com/survey-core@1.8.77/modern.min.css" type="text/css" rel="stylesheet"/>



<div class="card">
 <div class="card-body">
  <% if current_usuario.rol == 'T' %>
     <i class="fas fa-arrow-left"></i>&nbsp;<%= link_to("Regresar", evaltecnico_path, data: {turbolinks:false} ) %>&nbsp
  <% else %>
    <i class="fas fa-arrow-left"></i>&nbsp;<%= link_to("Regresar", evaltecnico_path, data: {turbolinks:false} ) %>&nbsp
  <% end %>
</div> 
</div> 



<div class="d-flex p-2">
  <div class="card">
     <div class="card-body">
        <b>Técnico:</b> <%= (@academico.persona.paterno||'')+" "+(@academico.persona.materno)+" "+(@academico.persona.nombre) %>
        <br>
        <b>Actividad:</b> <%= @sustantiva.descripcion %> <b>(<%= @sustantiva.porcentaje %>%)</b>
        <br>
        
     </div>
  </div>
</div>
   <% if @tipo == 'C'%>
      <b>Cuestionario de Calidad</b>
   <% end %>
   <% if @tipo == 'C'%>
      <%= render partial: "listado/calidad", layout: "crud/layout" %>
   <% end %>
   <% if @tipo == 'E'%>
      <b>Cuestionario de Eficiencia</b>
   <% end %>
   <% if @tipo == 'E'%>
      <%= render partial: "listado/eficiencia", layout: "crud/layout" %>
   <% end %>