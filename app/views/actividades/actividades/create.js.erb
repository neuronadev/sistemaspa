alert("Error: Falta información, favor de revisar.")
var form = document.querySelector("form");
<%  
   @actividad.errors.each do |attribute, message|
          @att_mod = '' 
          if attribute.to_s != 'autores' && attribute.to_s != 'documentos'
                if attribute.to_s.include? "." 
                    @att_mod = attribute.to_s.split(".")[1] 
                else
                    @att_mod = attribute
                end 
                @att_mod = "[name*='#{@att_mod}']"        
          else
                if attribute.to_s == 'autores'
                    @att_mod = "[id*='#{attribute.to_s}']"  
                end
                if attribute.to_s == 'documentos'
                    @att_mod = "[id*='#{attribute.to_s}']"  
                end    
          end     
%>
          var el = form.querySelector("<%= @att_mod.html_safe %>")
          if ( el != null ){
              el.style.borderColor = "red"
              el.style.color = 'red'
          }    
<% 
     end

%>