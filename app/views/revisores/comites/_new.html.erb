<%= form_with model: [:revisores, @comite] do |form| %>
   <div class="row" style=" display: flex; align-items: center; justify-content: center;"> 
     <div class="col-8">
        <div class="row p-1">
                <div class="col-2">Académico:</div>
                <div class="col"> <%= form.select :academico_id, Academico.includes(:persona).where('personas.estado':'A').all.order('personas.nombre').map{ |item| [item.persona.nomcompleto, item.id] } %>  </div>
        </div>
        <div class="row p-1">
                <div class="col-2">Rol:</div>
                <div class="col"> <%= form.select :catevaluador_id, Catevaluador.all.map{ |item| [item.descripcion, item.id] } %>  </div>
        </div>
        <div class="row p-1">
                <div class="col centrarel"> 
                    <%= form.submit "Aceptar"%>
                </div>   
        </div>
      </div>  
    </div>
<% end %>
